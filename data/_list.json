import list from '../_list.json';

export default {
    data() {
        return {
            levels: list
        };
    },
    template: `
        <div>
            <h1>List</h1>
            <div v-for="level in levels" :key="level">
                {{ level }}
            </div>
        </div>
    `
};
